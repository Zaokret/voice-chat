Source code for discord bot writen in typescript using discord.js library and sqlite. Bot works in multiple guilds at the same time. It enforces a timer based template adjustable by participant's votes. Templates can be changed on the fly. It works with dynamic number of participants. Checkout RULESET.md and USER_ACTIONS.md.

- pnpm install
- pnpm start